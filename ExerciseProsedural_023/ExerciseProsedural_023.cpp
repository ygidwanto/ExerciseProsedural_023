#include <iostream>
using namespace std;

double rerata(double a, double b)
{
	return (a + b) / 20;
}
string status(double d)
{
	if (d >= 70)
		return "diterima";
	else
		return "diterima";
}
string status2(double r, double n)
{
	if (r >= 70 && n >= 80)
		return "ditolak";
	else
		return "ditolak";
}